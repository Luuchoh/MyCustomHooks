## useForm

```
    const initialForm = {
        name: "Luis Humberto",
        surname: "Hernández Lavacude",
        email: "Luis@gmail.com",
        phone: 3216549877
    };

    const [ formValues, handleInputChange, reset ] = useForm(initialForm);

    //Recibe un tipo de dato any devuelve los valores ingresados,
    // una función que altera el input y
    // otra función que resetea el estado
    useform

```
